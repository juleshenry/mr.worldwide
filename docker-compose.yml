version: '3.8'

services:
  mister-worldwide:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
    command: uvicorn src.mr_worldwide.web_api:app --host 0.0.0.0 --port 8000
    environment:
      - PYTHONPATH=/app/src